{
	"format_version": "1.10.0",
	"particle_effect": {
		"description": {
			"identifier": "relics_and_foes:city_petal_atlas",
			"basic_render_parameters": {
				"material": "particles_alpha",
				"texture": "relics_and_foes:boss_petal_atlas"
			}
		},
		"components": {
			"minecraft:emitter_initialization": {
				"creation_expression": "v.random_data1=math.random(0,1);v.random_data2=math.random(0,1);v.random_data3=math.random(0,1);v.speed_y=math.random(0.8,1.2);v.color=math.random(0.4,0.65);v.wind_speed=math.random(0.85,1.15);v.speed=math.random(1.5,3);"
			},
			"minecraft:emitter_rate_instant": {
				"num_particles": 2
			},
			"minecraft:emitter_lifetime_once": {
				"active_time": 1
			},
			"minecraft:emitter_shape_box": {
				"offset": ["math.random(-16,2)", "math.random(-5,12)", "math.random(-16,2)"],
				"half_dimensions": [0.5, 0, 0.5],
				"direction": ["math.random(0.9,1.1)", 0, "math.random(0.9,1.1)"]
			},
			"minecraft:particle_lifetime_expression": {
				"max_lifetime": "math.random(7,11)"
			},
			"minecraft:particle_initial_spin": {
				"rotation": "math.random(0,360)",
				"rotation_rate": "math.random(-300,300)"
			},
			"minecraft:particle_initial_speed": "math.random(3,4)",
			"minecraft:particle_motion_dynamic": {
				"linear_acceleration": ["(math.sin(v.particle_age*180*v.wind_speed)+1)*2*v.speed", "-math.sin(v.particle_age*150*v.wind_speed)*v.speed_y-0.8", "(math.sin(v.particle_age*180*v.wind_speed)+1)*2*v.speed"],
				"linear_drag_coefficient": 1
			},
			"minecraft:particle_appearance_billboard": {
				"size": ["v.random_data1 > 0.5 ? 0.05 : 0.075", "v.random_data1 > 0.5 ? 0.05 : 0.075"],
				"facing_camera_mode": "lookat_direction",
				"direction": {
					"mode": "custom",
					"custom_direction": ["math.sin(v.particle_age*250)*300", "math.sin(v.particle_age*250)*300", "math.cos(v.particle_age*250)*300"]
				},
				"uv": {
					"texture_width": 12,
					"texture_height": 9,
					"uv": ["math.floor(v.random_data2 * 4) * 3", "math.floor(v.random_data3 * 3) * 3"],
					"uv_size": [3, 3]
				}
			},
			"minecraft:particle_appearance_lighting": {},
			"minecraft:particle_appearance_tinting": {
				"color": [0.08, "v.color", 0.07, 1]
			}
		}
	}
}